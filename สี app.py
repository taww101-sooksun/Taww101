import streamlit as st
import google.generativeai as genai

# ส่วนการตั้งค่า API
try:
    api_key = st.secrets["GEMINI_API_KEY"]
    genai.configure(api_key=api_key)
    
    # เปลี่ยนเป็นรุ่นล่าสุดเพื่อให้หายแดง 404
    model = genai.GenerativeModel('gemini-1.5-flash') 
    
except Exception as e:
    st.error(f"ตรวจพบข้อผิดพลาด: {e}")
    st.stop()
 ส่วนหน้าตาแอป (UI) ---
st.title("🤖 AI Chat: สไตล์อยู่นิ่งๆ ไม่เจ็บตัว")
st.write("แอปนี้จะช่วยร่างคำตอบให้คุณเลือก โดยที่คุณไม่ต้องพิมพ์เอง")

# จำลองสถานการณ์: มีข้อความส่งมาจากเพื่อน
# (ในอนาคตเราจะเปลี่ยนตรงนี้ให้ดึงจาก Database จริง)
st.divider()
friend_input = st.text_input("💬 ข้อความที่ได้รับจากเพื่อน:", placeholder="เช่น... พรุ่งนี้ไปเที่ยวกันไหม?")

if friend_input:
    with st.spinner('AI กำลังคิดคำตอบเนียนๆ ให้...'):
        try:
            # สั่ง AI ให้ร่างคำตอบ 3 สไตล์
            prompt = f"""
            เพื่อนส่งแชตมาว่า: "{friend_input}"
            ช่วยร่างคำตอบสั้นๆ 3 แบบเพื่อตอบกลับ:
            แบบที่ 1: เห็นด้วย/ตกลง
            แบบที่ 2: ปฏิเสธแบบรักษาน้ำใจ
            แบบที่ 3: ตอบแบบกวนๆ/ขี้เกียจ
            (ตอบมาเฉพาะข้อความที่จะใช้ส่งเท่านั้น แยกเป็นข้อๆ)
            """
            response = model.generate_content(prompt)
            suggestions = response.text.strip().split('\n')
            
            # กรองเอาเฉพาะข้อความ (กรณี AI ใส่เลขข้อมาด้วย)
            clean_suggestions = [s.split('. ')[-1] if '. ' in s else s for s in suggestions if s.strip()]

            st.write("---")
            st.subheader("🎯 เลือกคำตอบที่โดนใจ:")
            
            # สร้างปุ่ม 3 ปุ่มให้เลือกกด
            for choice in clean_suggestions[:3]: # เอาแค่ 3 อันแรก
                if st.button(choice, use_container_width=True):
                    st.success(f"✅ ส่งข้อความเรียบร้อย: {choice}")
                    st.balloons() # แสดงความยินดีที่ส่งสำเร็จแบบไม่ต้องพิมพ์เอง
                    
        except Exception as e:
            st.error(f"เกิดข้อผิดพลาดในการเรียก AI: {e}")

st.divider()
st.caption("พัฒนาโดย AI เพื่อนยาก | สโลแกน: import streamlit as st
import google.generativeai as genai

try:
    api_key = st.secrets["GEMINI_API_KEY"]
    genai.configure(api_key=api_key)
    # แก้ไขชื่อรุ่นตรงนี้เพื่อให้หายแดง 404
    model = genai.GenerativeModel('gemini-1.5-flash') 
except Exception as e:
    st.error(f"ตรวจพบข้อผิดพลาด: {e}")
    st.stop()
