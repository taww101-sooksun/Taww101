import streamlit as st
import google.generativeai as genai

# --- 1. ตั้งค่าหน้าตาแอป ---
st.set_page_config(page_title="AI Chat VIP", layout="centered")

# --- 2. ตั้งค่ากุญแจและรุ่น AI (ระบุรุ่นชัดเจน ไม่ต้องสุ่ม) ---
try:
    # ดึงกุญแจดอกเดียวที่คุณใส่ไว้ใน Secrets
    api_key = st.secrets["GEMINI_API_KEY"] 
    genai.configure(api_key=api_key)
    
    # ระบุรุ่นที่กุญแจคุณรองรับและเสถียรที่สุด
    model = genai.GenerativeModel('models/gemini-2.0-flash')
except Exception as e:
    st.error(f"🚨 กุญแจมีปัญหา: {e}")
    st.stop()

# --- 3. ระบบหน้าด่าน (กันคนมั่ว) ---
st.title("🤖 AI Chat: สไตล์เพื่อนตัวจริง")

user_email = st.text_input("ระบุ Gmail ของคุณเพื่อยืนยันตัวตน:")

# ลิสต์ Gmail เพื่อนที่อนุญาต (คุณมาแก้ตรงนี้เพิ่มเอาเองนะ)
allowed_friends = ["yourname@gmail.com", "friend@gmail.com"]

if user_email:
    if user_email in allowed_friends:
        st.success(f"ยืนยันตัวตนสำเร็จ: ยินดีต้อนรับ {user_email}")
        
        st.divider()
        friend_msg = st.text_input("💬 ข้อความที่เพื่อนส่งมา:")
        
        if friend_msg:
            with st.spinner('AI กำลังร่างคำตอบ...'):
                try:
                    prompt = f"เพื่อนส่งแชตมาว่า: '{friend_msg}' ช่วยร่างคำตอบสั้นๆ 3 แบบ: 1.ตกลง 2.ไม่ว่าง 3.กวนๆ"
                    response = model.generate_content(prompt)
                    st.write("---")
                    st.subheader("🎯 เลือกคำตอบที่ชอบ:")
                    st.write(response.text)
                except Exception as e:
                    st.error(f"เกิดข้อผิดพลาดในการเรียก AI: {e}")
    else:
        st.error("Gmail นี้ไม่มีสิทธิ์เข้าใช้งานครับ")
