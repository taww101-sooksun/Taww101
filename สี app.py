import streamlit as st
import firebase_admin
from firebase_admin import credentials, db
import datetime

with tab2:
    st.header("📊 สถานะและการติดตามพิกัด")
    
    # 1. ดึงข้อมูลทุกคนจาก Firebase
    users_ref = db.reference('users').get()
    
    if users_ref:
        import pandas as pd
        map_data = []
        
        for uid, info in users_ref.items():
            # เช็กว่ามีพิกัดส่งมาจริงไหม (ถ้าไม่มีให้ข้ามไปก่อน)
            if 'lat' in info and 'lon' in info:
                map_data.append({
                    'name': uid,
                    'latitude': info['lat'],
                    'longitude': info['lon']
                })
        
        if map_data:
            df = pd.DataFrame(map_data)
            # 2. คำสั่งวาดแผนที่ (ทำได้จริง!)
            st.map(df) 
            # 3. โชว์ตารางรายละเอียดข้างล่างแผนที่
            st.table(df)
        else:
            st.warning("พบบันทึกชื่อผู้ใช้ แต่ยังไม่มีข้อมูลพิกัด (Lat/Lon) ส่งเข้ามาครับ")
    else:
        st.write("ยังไม่มีใครเชื่อมต่อเข้ามาครับ")

    
    if st.button("Start Journey"):
        if user_id:
            # จำลองการดึงเวลา (ในแอปจริงจะใช้พิกัดคำนวณ Timezone)
            now = datetime.datetime.now()
            current_hour = now.hour
            
                        # ตรรกะเลือกเพลง (Logic) - ตัด music/ ออกเพราะไฟล์วางอยู่ข้างนอก
            if 6 <= current_hour < 12:
                song_path = "test_morning.mp3" # ไม่มี music/ แล้ว
                status = "เช้าอันสดใส"
            else:
                song_path = "test_evening.mp3" # ไม่มี music/ แล้ว
                status = "เย็นที่ผ่อนคลาย"

            # ส่วนการเล่นเพลง
            import os
            if os.path.exists(song_path):
                st.audio(song_path)
                st.success(f"🎵 เจอไฟล์แล้ว! กำลังเล่นโหมด: {status}")
            else:
                st.error(f"❌ ยังหาไฟล์ '{song_path}' ไม่เจอ")
                # บรรทัดนี้จะช่วยบอกว่าจริงๆ แล้วตอนนี้ใน GitHub มีไฟล์ชื่ออะไรบ้าง
                st.write("ไฟล์ที่ระบบมองเห็นตอนนี้คือ:", os.listdir("."))


            # ส่งค่าขึ้น Firebase (ทำได้จริง!)
            ref = db.reference(f'users/{user_id}')
            ref.set({
                'last_seen': str(now),
                'status': status,
                'song': song_path
            })

            st.success(f"เชื่อมต่อสำเร็จ! ขณะนี้เป็นเวลา {now.strftime('%H:%M')}")
            st.write(f"กำลังบำบัดด้วยโหมด: **{status}**")
            
            # เครื่องเล่นเพลง
            st.audio(song_path)
        else:
            st.warning("กรุณาใส่ชื่อก่อนนะครับเพื่อน")

with tab2:
    st.header("สถานะเพื่อนๆ ทั้งหมด")
    # ดึงข้อมูลทุกคนจาก Firebase มาโชว์
    users_ref = db.reference('users').get()
    if users_ref:
        st.table(users_ref) # โชว์เป็นตารางให้ดูง่ายๆ เลย
    else:
        st.write("ยังไม่มีใครเชื่อมต่อเข้ามาครับ")

